USE HIGH_AGE_ID;

WITH B AS(
SELECT DISTINCT ID FROM ID2003
)
SELECT COUNT(ID) FROM B

USE HIGH_AGE_ID;

WITH B AS(
SELECT DISTINCT ID FROM ID2004
)
SELECT COUNT(ID) FROM B

USE HIGH_AGE_ID;

WITH B AS(
SELECT DISTINCT ID FROM ID2005
)
SELECT COUNT(ID) FROM B

USE HIGH_AGE_ID;

WITH B AS(
SELECT DISTINCT ID FROM ID2006
)
SELECT COUNT(ID) FROM B

USE HIGH_AGE_ID;

WITH B AS(
SELECT DISTINCT ID FROM ID2007
)
SELECT COUNT(ID) FROM B

USE HIGH_AGE_ID;

WITH B AS(
SELECT DISTINCT ID FROM ID2008
)
SELECT COUNT(ID) FROM B

USE HIGH_AGE_ID;

WITH B AS(
SELECT DISTINCT ID FROM ID2009
)
SELECT COUNT(ID) FROM B

USE HIGH_AGE_ID;

WITH B AS(
SELECT DISTINCT ID FROM ID2010
)
SELECT COUNT(ID) FROM B;

WITH B AS(
SELECT DISTINCT ID FROM ID2011
)
SELECT COUNT(ID) FROM B;

WITH B AS(
SELECT DISTINCT ID FROM ID2012
)
SELECT COUNT(ID) FROM B;

WITH B AS(
SELECT DISTINCT ID FROM ID2013
)
SELECT COUNT(ID) FROM B;